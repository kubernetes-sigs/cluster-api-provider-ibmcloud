---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: rokscontrolplanes.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: ROKSControlPlane
    listKind: ROKSControlPlaneList
    plural: rokscontrolplanes
    singular: rokscontrolplane
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster name
      jsonPath: .metadata.name
      name: Cluster
      type: string
    - description: Cluster ready status
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: Cluster state
      jsonPath: .status.state
      name: State
      type: string
    - description: IBM Cloud cluster ID
      jsonPath: .status.clusterID
      name: ClusterID
      type: string
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: ROKSControlPlane is the Schema for the rokscontrolplanes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ROKSControlPlane
            properties:
              controlPlaneEndpoint:
                description: ControlPlaneEndpoint represents the endpoint used to
                  communicate with the control plane
                minProperties: 1
                properties:
                  host:
                    description: host is the hostname on which the API server is serving.
                    maxLength: 512
                    minLength: 1
                    type: string
                  port:
                    description: port is the port on which the API server is serving.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              defaultWorkerPool:
                description: DefaultWorkerPool defines the default worker pool configuration
                properties:
                  flavor:
                    description: Flavor is the machine type
                    type: string
                  name:
                    default: default
                    description: Name is the name of the worker pool
                    type: string
                  workerCount:
                    default: 3
                    description: WorkerCount is the number of workers
                    minimum: 1
                    type: integer
                  zones:
                    description: Zones defines the zones for the worker pool
                    items:
                      description: WorkerPoolZoneSpec defines a zone for a worker
                        pool
                      properties:
                        id:
                          description: ID is the zone ID
                          type: string
                        subnetID:
                          description: SubnetID is the subnet ID (for VPC clusters)
                          type: string
                      required:
                      - id
                      type: object
                    minItems: 1
                    type: array
                required:
                - flavor
                - zones
                type: object
              location:
                description: Location is the datacenter or zone where the cluster
                  will be created
                type: string
              name:
                description: Name is the name of the ROKS cluster
                type: string
              network:
                description: Networking configuration
                properties:
                  podSubnet:
                    default: 172.30.0.0/16
                    description: PodSubnet is the subnet for pods
                    type: string
                  privateServiceEndpoint:
                    default: true
                    description: PrivateServiceEndpoint enables private service endpoint
                    type: boolean
                  publicServiceEndpoint:
                    default: true
                    description: PublicServiceEndpoint enables public service endpoint
                    type: boolean
                  serviceSubnet:
                    default: 172.21.0.0/16
                    description: ServiceSubnet is the subnet for services
                    type: string
                type: object
              openshift:
                description: OpenShift specific configuration
                properties:
                  cosInstanceCRN:
                    description: CosInstanceCRN is the CRN of the COS instance for
                      image registry
                    type: string
                  entitlement:
                    description: Entitlement specifies the OpenShift entitlement
                    enum:
                    - cloud_pak
                    - ocp_entitled
                    type: string
                required:
                - cosInstanceCRN
                type: object
              openshiftVersion:
                description: OpenshiftVersion is the OpenShift version to install
                pattern: ^\d+\.\d+\.\d+_openshift$
                type: string
              resourceGroupID:
                description: ResourceGroupID is the ID of the resource group
                type: string
              security:
                description: Security and encryption
                properties:
                  diskEncryption:
                    default: true
                    description: DiskEncryption enables disk encryption
                    type: boolean
                  kms:
                    description: KMS contains Key Management Service configuration
                    properties:
                      instanceID:
                        description: InstanceID is the KMS instance ID
                        type: string
                      rootKeyID:
                        description: RootKeyID is the root key ID
                        type: string
                    required:
                    - instanceID
                    - rootKeyID
                    type: object
                type: object
              vpc:
                description: VPC configuration
                properties:
                  subnetID:
                    description: SubnetID is the ID of the subnet
                    type: string
                  vpcID:
                    description: VPCID is the ID of the VPC
                    type: string
                required:
                - subnetID
                - vpcID
                type: object
            required:
            - defaultWorkerPool
            - location
            - name
            - openshift
            - openshiftVersion
            - resourceGroupID
            - vpc
            type: object
          status:
            description: status defines the observed state of ROKSControlPlane
            properties:
              clusterID:
                description: ClusterID is the IBM Cloud cluster ID
                type: string
              conditions:
                description: Conditions defines current service state
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              failureMessage:
                description: FailureMessage indicates a fatal problem reconciling
                  the state
                type: string
              failureReason:
                description: FailureReason indicates there is a fatal problem reconciling
                  the state
                type: string
              ingressHostname:
                description: IngressHostname is the ingress hostname
                type: string
              ingressSecretName:
                description: IngressSecretName is the ingress secret name
                type: string
              masterStatus:
                description: MasterStatus is the master status
                type: string
              masterURL:
                description: MasterURL is the master API endpoint
                type: string
              ready:
                description: Ready indicates the cluster is ready
                type: boolean
              state:
                description: State is the cluster state
                type: string
              v1beta2:
                description: V1beta2 groups all the fields that will be added or modified
                  in ROKSControlPlane's status with the V1Beta2 version.
                properties:
                  conditions:
                    description: Conditions represents the observations of a ROKSControlPlane's
                      current state.
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    maxItems: 32
                    type: array
                    x-kubernetes-list-map-keys:
                    - type
                    x-kubernetes-list-type: map
                type: object
              workerCount:
                description: WorkerCount is the current number of workers
                type: integer
            required:
            - ready
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
